apiVersion: batch/v1
kind: Job
metadata:
  name: test-job
  labels:
    rank: worker
spec:
  template:
    spec:
      containers:
        - name: test-project
          image: brenmcnamara/test-project
          command: ["python", "./main.py", "run", "bow_classifier.train"]
          env:
            - name: API_ENDPOINT
              value: mlwebservice.default.svc.cluster.local:50051
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: primary.secrets
                  key: AWS_ACCESS_KEY_ID
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: primary.secrets
                  key: AWS_SECRET_ACCESS_KEY
            - name: IMAGE_NAME
              value: 9point/ml-project-news-classifier
            - name: LOCAL_CACHE_DIR
              value: /tmp/s3-cache
            - name: PROJECT_NAME
              value: news-classifier
            - name: SPACES_BUCKET
              valueFrom:
                configMapKeyRef:
                  name: primary.configs
                  key: SPACES_BUCKET
            - name: SPACES_REGION
              valueFrom:
                configMapKeyRef:
                  name: primary.configs
                  key: SPACES_REGION
      restartPolicy: Never
  backoffLimit: 1
